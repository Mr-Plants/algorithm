/*
冒泡排序
思路：
循环长度为n的数组，冒泡n次
每次冒泡，对比自己和下一位元素，如果比下一位大，就向右冒泡（交换位置）
 */

function bubbleSort(arr) {

  for (let i = 0; i < arr.length; i++) {
    let a = arr[i];
    let b = arr[i + 1];
    if (a > b) {

    }
  }
}
